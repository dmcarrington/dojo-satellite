version: "3.2"

services:
  blocksat:
    image: "blocksat"
    container_name: blocksat2
    build:
      context: ./blocksat2
    env_file:
      - ./.env
      - ./conf/docker-common.conf
      - ./conf/docker-blocksat.conf
    restart: always
    privileged: true
    command: "sdr"
    expose:
      - "8333"
      - "28256"
      - "9501"
      - "9502"
    volumes:
      - /dev/bus/usb:/dev/bus/usb
    networks:
      dojonet:
        ipv4_address: 172.28.1.8
    
  node:
    depends_on:
      - blocksat2

  

